Crée pour moi une page web minimale correspondant à l’Étape 1 du jeu des Tours de Hanoï.
Je veux uniquement :

Une zone affichant clairement 3 tours (3 piliers visibles).

Un champ permettant de choisir le nombre d’anneaux (input number).

Un bouton “Démarrer”.

Donne-moi le code complet en trois parties :

HTML (structure de la page)

CSS (apparence simple pour voir les 3 tours alignées)

JavaScript (uniquement une fonction startGame() reliée au bouton, mais vide pour l’instant)

Pas de génération d’anneaux, pas d’interaction.